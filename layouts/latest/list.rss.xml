<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }} (近期更新)</title>
    <link>{{ .Site.BaseURL }}</link>
    <description>The latest 10 posts from {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>
    {{ with .Site.LanguageCode }}<language>{{.}}</language>{{end}}
    {{- with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{ range first 10 .Site.RegularPages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      <guid>{{ .Permalink }}</guid>
      {{- $content := .Content -}}
      {{- $sidenotes := findRE `<span class="sidenote-content">.*?</span>` $content -}}
      {{- if $sidenotes -}}
        {{- $content = replaceRE `<span class="sidenote-content">.*?</span>` "" $content -}}
        {{- $footnotes := `<hr style="border: 0; border-top: 1px solid #eee; margin: 20px 0;" /><div style="line-height: 2; font-size: 0.9em; color: #666;">` -}}
        {{- range $sidenotes -}}
          {{- $text := replaceRE `<span class="sidenote-content">(.*?)</span>` "$1" . -}}
          {{- $footnotes = printf "%s<p style=\"margin: 0.5em 0; line-height: 2;\">%s</p>" $footnotes $text -}}
        {{- end -}}
        {{- $footnotes = printf "%s</div>" $footnotes -}}
        {{- $content = printf "%s%s" $content $footnotes -}}
      {{- end -}}
      <description>{{ (printf "%s<br><br><div style=\"text-align: right;\"><a href=\"%s\" style=\"text-decoration: underline;\">看完了，进来坐坐？</a></div>" $content .Permalink) | html }}</description>
    </item>
    {{ end }}
  </channel>
</rss>
